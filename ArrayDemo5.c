#include <stdio.h>

int main()
{
    double dArr[] = {10.0, 20.0, 30.0, 40.0};

    printf("Size of dArr :%lu\n", sizeof(dArr));

    Arr++; 
    //this above syntax will generate error because the base address of an cannot be changed
